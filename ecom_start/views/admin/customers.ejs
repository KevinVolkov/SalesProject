<!-- 
Module name: /views/admin/customers.ejs
Date of the code (latest update): 12/06/24
-----------------------------------------------------------------------------------------------
Programmers: Kevin Volkov,  Irvin Merino, Joaquin Banting /students, CSUN COMP 380, Group #6/
-----------------------------------------------------------------------------------------------
Description: this module displays a list of all registered customers and their order histories.
- Provides an overview of customer details, including name, email, and order records.
- Allows 'admin' to monitor and manage customer list
-->


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin's Customers Management</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <%- include('../partials/header') %>
    <div class="container">



<h1>Customers</h1>
<ul>
    <% customers.forEach(customer => { %>
        <li><%= customer.name %> (<%= customer.email %>)


            

            <% if (customer.orders && customer.orders.length > 0) { %>
                <ul>
                    <% customer.orders.forEach((order, index) => { %>
                        <li>
                            <strong>Order <%= index + 1 %>:</strong>
                            <%= JSON.parse(order).map(item => `${item.name} (x${item.quantity})`).join(', ') %>
                        </li>
                    <% }); %>
                </ul>
            <% } else { %>
                <p>No orders yet</p>
            <% } %>







            <form action="/admin/customers/delete/<%= customer.id %>" method="POST">
                <button type="submit">Delete</button>
            </form>
        </li>
    <% }) %>
</ul>


</div>
</body>
</html>
